# Obsidian 多平台同步迁移指南

## 📋 当前问题

您的 vault 在多平台使用时遇到插件同步问题，主要原因：

1. **桌面专用插件**在移动端（iOS/iPad）不可用，导致加载错误
2. **插件代码同步**增加仓库体积，且不同平台可能需要不同版本
3. **插件数据文件**包含本地路径和 API 密钥，不应跨设备共享

## 🎯 解决方案：分离式同步

### 核心思路

- ✅ **同步**：插件列表（`community-plugins.json`）
- ❌ **不同步**：插件代码目录（`.obsidian/plugins/`）
- 📦 **工作流**：各平台根据插件列表自行安装

## 🔧 迁移步骤

### 步骤 1：备份当前配置

```bash
# 备份整个 .obsidian 目录
cp -r .obsidian .obsidian.backup.$(date +%Y%m%d)

# 或仅备份插件
cp -r .obsidian/plugins .obsidian/plugins.backup
```

### 步骤 2：更新 .gitignore

```bash
# 使用推荐配置
cp .gitignore.recommended .gitignore

# 或手动编辑 .gitignore，添加：
# .obsidian/plugins/
# !.obsidian/community-plugins.json
# !.obsidian/core-plugins.json
```

### 步骤 3：清理 Git 仓库中的插件

```bash
# 从 Git 历史中移除插件目录（保留本地文件）
git rm -r --cached .obsidian/plugins/

# 恢复插件列表文件
git add -f .obsidian/community-plugins.json
git add -f .obsidian/core-plugins.json

# 提交更改
git commit -m "chore: 移除插件代码同步，仅保留插件列表

- 避免跨平台插件兼容性问题
- 减少仓库体积
- 各平台根据 community-plugins.json 自行安装"

# 推送到远程
git push origin main
```

### 步骤 4：在其他设备上处理

#### 在 Linux/Windows 设备：

```bash
# 拉取最新更改
git pull origin main

# 插件目录现在是本地化的，不会被 Git 追踪
# 已安装的插件会保留在本地
```

#### 在 iOS/iPad 设备：

1. **使用 Obsidian Git 插件**：
   - 拉取最新更改（插件代码会被删除）
   - 打开 `社区插件` → `浏览`
   - 根据 `community-plugins.json` 手动安装兼容的插件

2. **或使用 Obsidian Sync**：
   - 不启用 `同步插件` 选项
   - 手动安装需要的插件

## 📱 各平台插件安装指南

### 主设备（Windows/Linux）

保留所有插件，包括桌面专用：
- ✅ Shell commands
- ✅ Local REST API
- ✅ Claudian
- ✅ VSCode Editor
- ✅ 所有其他插件

### 移动设备（iOS/iPad）

仅安装移动端兼容插件，**跳过以下插件**：
- ❌ Shell commands（桌面专用）
- ❌ Local REST API（桌面专用）
- ❌ Claudian（桌面专用）
- ❌ VSCode Editor（桌面专用）

**推荐安装**：
- ✅ Dataview
- ✅ Templater
- ✅ Excalidraw
- ✅ Style Settings
- ✅ Mind Map
- ✅ Image Toolkit
- ✅ Table Editor
- ✅ PDF++
- ✅ Full Calendar

## 🔍 验证配置

### 检查 Git 状态

```bash
# 应该看到 .obsidian/plugins/ 不再被追踪
git status .obsidian/

# 应该只有这两个文件被追踪
git ls-files .obsidian/ | grep plugins
# 输出：
# .obsidian/community-plugins.json
# .obsidian/core-plugins.json
```

### 测试跨平台同步

1. 在主设备添加新插件
2. 插件列表自动更新到 `community-plugins.json`
3. 提交并推送更改
4. 在其他设备拉取
5. 根据更新后的列表手动安装新插件

## 🎨 可选：主题同步

如果您使用自定义主题，可以同步主题文件：

```gitignore
# 在 .gitignore 中添加例外
!.obsidian/themes/
```

**注意**：某些主题的 CSS 可能在移动端显示不佳。

## 💾 插件数据同步（高级）

某些插件的配置可能需要跨设备共享（如 Templater 模板路径）。可以选择性同步：

```gitignore
# 同步 Templater 配置
!.obsidian/plugins/templater-obsidian/data.json

# 同步 Dataview 配置
!.obsidian/plugins/dataview/data.json
```

**警告**：
- 仅同步不包含本地路径的配置
- 不要同步包含 API 密钥的配置（如 Smart Connections）

## 🚨 常见问题

### Q1: 拉取后插件消失了？

插件代码现在是本地化的，不会被 Git 删除。如果确实消失：
```bash
# 恢复备份
cp -r .obsidian.backup.YYYYMMDD/plugins .obsidian/
```

### Q2: community-plugins.json 冲突？

```bash
# 选择保留自己的版本
git checkout --ours .obsidian/community-plugins.json

# 或选择远程版本
git checkout --theirs .obsidian/community-plugins.json

# 手动合并后
git add .obsidian/community-plugins.json
git commit
```

### Q3: 如何知道哪些插件是桌面专用？

查看插件的 `manifest.json`：
```bash
grep -r "isDesktopOnly.*true" .obsidian/plugins/*/manifest.json
```

## 📚 参考资料

- [Obsidian 官方文档 - 社区插件](https://help.obsidian.md/Extending+Obsidian/Community+plugins)
- [Git .gitignore 文档](https://git-scm.com/docs/gitignore)
- [Obsidian Forum - Syncing Plugins](https://forum.obsidian.md/tag/sync)

## 🎯 总结

采用分离式同步策略后：
- ✅ 避免跨平台插件兼容性问题
- ✅ 减少 Git 仓库体积
- ✅ 每个平台使用适配的插件版本
- ✅ 插件列表作为参考，保持配置一致性

**trade-off**：需要在新设备上手动安装插件一次，但这是跨平台使用的最佳实践。
