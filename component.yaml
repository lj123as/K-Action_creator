# Action Creator 组件声明（目录名 K-Action_creator）
id: action-creator
dir:
  - cognition/K-Action_creator
  - action/K-Action_creator
version: 1.0.0
provides:
  - action.create      # 新 action subsystem 骨架生成（含 component.yaml）
requires:
  - knowledge.query    # 消费 ActionCandidate v1
